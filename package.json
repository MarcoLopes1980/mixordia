{
  "name": "mixordia",
  "version": "1.0.0",
  "description": "Alienarium website with contact information",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:debug": "playwright test --debug"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
